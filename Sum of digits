#include <stdio.h>

int main()
{   
    int i,num,sum=0;
    printf("Enter number");
    scanf("%d",&num);
    while(i<num)
    {
        sum=num%10;
        num=num/10;
    }
    printf("%d",sum);
    return 0;
}
